library(testthat)

test_that("sensible objects are returned", {
    expect_that(raadtools:::graticule(), is_a("SpatialLinesDataFrame"))
    ##expect_that(graticule(labels = TRUE), is_a("SpatialPointsDataFrame"))
})
