% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-oisst.R
\name{read_oisst_monthly}
\alias{read_oisst_monthly}
\title{Read OISST monthly sea surface temperature}
\usage{
read_oisst_monthly(
  date,
  xylim = NULL,
  lon180 = TRUE,
  varname = c("sst", "anom", "err", "ice"),
  latest = TRUE,
  returnfiles = FALSE,
  ...,
  inputfiles = NULL
)
}
\arguments{
\item{date}{date or dates to read (character, Date, or POSIXct). If missing,
returns earliest or latest depending on \code{latest} argument.}

\item{xylim}{extent to crop, anything acceptable to \code{terra::ext()}.
Applied after rotation if \code{lon180 = TRUE}.}

\item{lon180}{logical, convert from Pacific (0,360) to Atlantic (-180,180) view?
Default \code{TRUE}.}

\item{varname}{variable to read: one of "sst", "anom", "err", "ice".}

\item{latest}{if \code{TRUE} and \code{date} missing, return latest available;
if \code{FALSE}, return earliest.}

\item{returnfiles}{if \code{TRUE}, return the file catalog instead of reading data.}

\item{...}{currently ignored.}

\item{inputfiles}{optionally provide a pre-filtered file catalog (tibble with
\code{date} and \code{fullname} columns).}
}
\value{
\code{SpatRaster} with time dimension set to file dates, or tibble if
\code{returnfiles = TRUE}.
}
\description{
Read NOAA Optimum Interpolation Sea Surface Temperature (OISST) v2.1 monthly data.
}
\details{
Monthly files contain the same variables as daily files. Date matching uses
a 28-day tolerance window.
}
\seealso{
\code{\link{read_oisst_daily}} for daily data
}
