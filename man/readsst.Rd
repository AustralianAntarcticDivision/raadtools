% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat-sst.R, R/sst.R
\name{readsst}
\alias{readsst}
\alias{readsst_daily}
\title{Read sea surface temperature data}
\usage{
readsst(
  date,
  time.resolution = c("daily", "monthly"),
  xylim = NULL,
  lon180 = TRUE,
  varname = c("sst", "anom", "err", "ice"),
  setNA = TRUE,
  latest = TRUE,
  returnfiles = FALSE,
  ...,
  inputfiles = NULL
)

readsst_daily(
  date,
  xylim = NULL,
  lon180 = TRUE,
  varname = c("sst", "anom", "err", "ice"),
  setNA = TRUE,
  latest = TRUE,
  returnfiles = FALSE,
  ...,
  inputfiles = NULL
)

readsst(
  date,
  time.resolution = c("daily", "monthly"),
  xylim = NULL,
  lon180 = TRUE,
  varname = c("sst", "anom", "err", "ice"),
  setNA = TRUE,
  latest = TRUE,
  returnfiles = FALSE,
  ...,
  inputfiles = NULL
)
}
\arguments{
\item{date}{date or dates of data to read}

\item{time.resolution}{time resolution: "daily" or "monthly"}

\item{xylim}{spatial extents to crop from source data}

\item{lon180}{if TRUE (default), convert Pacific (0,360) to Atlantic (-180,180) view}

\item{varname}{variable to return from the data files, default is
"sst" or "anom", "err", "ice"}

\item{setNA}{mask out land values (ignored in new implementation, kept for API compatibility}

\item{latest}{if TRUE and date missing, return latest available}

\item{returnfiles}{if TRUE, return the file catalog instead of data}

\item{...}{
  Arguments passed on to \code{\link[=raadtools]{raadtools}}
  \describe{
    \item{\code{}}{}
  }}

\item{inputfiles}{optional pre-filtered file catalog}

\item{readall}{FALSE by default}
}
\value{
\code{RasterBrick} or \code{RasterLayer}, or tibble if \code{returnfiles = TRUE}

\code{\link[raster]{raster}} object
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#superseded}{\figure{lifecycle-superseded.svg}{options: alt='[Superseded]'}}}{\strong{[Superseded]}}

\code{readsst} has been superseded by \code{\link{read_oisst_daily}} and
\code{\link{read_oisst_monthly}}, which return terra \code{SpatRaster} objects.
This function is retained for backward compatibility and returns
\code{raster::brick} objects.

SST data read from files managed by
\code{\link{sstfiles}}. Dates are matched to file names by finding
the nearest match in time within a short duration. If \code{date}
is greater than length 1 then the sorted set of unique matches is
returned.
see Details in raadtools
}
\examples{
\dontrun{
# Legacy usage (still works)
sst <- readsst("2023-01-15")
class(sst)  # RasterLayer

# Modern usage (preferred)
sst <- read_oisst_daily("2023-01-15")
class(sst)  # SpatRaster
}
\dontrun{
## read one time slice and plot it up in preparation for reading a time series
d <- readsst()
plot(d)
## this step is interactive, draw a boundary on the plot
ext <- drawExtent()
## these can be created manually with xmin,xmax,ymin,ymax
## ext <- extent(-100, 150, -75, -30)
## now read a big chunk of data for this small region
dts <- seq(as.Date("2001-01-03"), by = "1 week", length = 100)
sst <- readsst(dts, xylim = ext)
}

}
\seealso{
\code{\link{read_oisst_daily}} for the modern terra-based daily reader
\code{\link{read_oisst_monthly}} for the modern terra-based monthly reader

\code{\link{icefiles}} for details on the repository of
data files, \code{\link[raster]{raster}} for the return value
}
