% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat-ice.R
\name{readice}
\alias{readice}
\alias{readice_daily}
\alias{readice_monthly}
\title{Read sea ice concentration data}
\usage{
readice(
  date,
  time.resolution = "daily",
  product = "nsidc",
  hemisphere = c("south", "north", "both"),
  xylim = NULL,
  setNA = TRUE,
  rescale = FALSE,
  latest = TRUE,
  returnfiles = FALSE,
  ...,
  inputfiles = NULL,
  resample = "bilinear"
)

readice_daily(
  date,
  time.resolution = "daily",
  product = "nsidc",
  hemisphere = c("south", "north", "both"),
  xylim = NULL,
  setNA = TRUE,
  rescale = FALSE,
  latest = TRUE,
  returnfiles = FALSE,
  ...,
  inputfiles = NULL,
  resample = "bilinear"
)

readice_monthly(
  date,
  time.resolution = "monthly",
  product = "nsidc",
  hemisphere = c("south", "north"),
  xylim = NULL,
  setNA = TRUE,
  rescale = TRUE,
  latest = TRUE,
  returnfiles = FALSE,
  ...,
  inputfiles = NULL
)
}
\arguments{
\item{date}{date or dates of data to read}

\item{time.resolution}{deprecated, use \code{readice_daily} or \code{readice_monthly}}

\item{product}{only "nsidc" supported}

\item{hemisphere}{"south", "north", or "both"}

\item{xylim}{spatial extents to crop, or a SpatRaster (or Raster*) template
to warp onto when \code{hemisphere = "both"}}

\item{setNA}{mask special values as NA}

\item{rescale}{ignored (v2 data is already scaled)}

\item{latest}{if TRUE and date missing, return latest available}

\item{returnfiles}{if TRUE, return file catalog}

\item{...}{passed to underlying reader}

\item{inputfiles}{optional pre-filtered file catalog}

\item{resample}{resampling method for warper (when hemisphere = "both")}
}
\value{
\code{SpatRaster}, or tibble if \code{returnfiles = TRUE}
}
\description{
\code{readice} picks a sensible default and returns a terra
\code{SpatRaster}. For the historical defaults pinned explicitly, see
\code{\link{read_nsidc_ice_daily}} and \code{\link{read_nsidc_ice_monthly}}.

The \code{hemisphere = "both"} functionality (warping both hemispheres to a
common grid) is only available through this function.
}
\seealso{
\code{\link{read_nsidc_ice_daily}} for modern terra-based daily reader
\code{\link{read_nsidc_ice_monthly}} for modern terra-based monthly reader
}
