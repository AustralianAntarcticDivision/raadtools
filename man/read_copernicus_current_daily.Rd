% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-currents.R
\name{read_copernicus_current_daily}
\alias{read_copernicus_current_daily}
\alias{read_aviso_current_daily}
\title{Read Copernicus ocean surface currents}
\usage{
read_copernicus_current_daily(
  date,
  xylim = NULL,
  lon180 = TRUE,
  magonly = FALSE,
  dironly = FALSE,
  uonly = FALSE,
  vonly = FALSE,
  latest = TRUE,
  returnfiles = FALSE,
  ...,
  inputfiles = NULL
)

read_aviso_current_daily(
  date,
  xylim = NULL,
  lon180 = TRUE,
  magonly = FALSE,
  dironly = FALSE,
  uonly = FALSE,
  vonly = FALSE,
  latest = TRUE,
  returnfiles = FALSE,
  ...,
  inputfiles = NULL
)
}
\arguments{
\item{date}{date or dates to read (character, Date, or POSIXct).}

\item{xylim}{extent to crop, anything acceptable to \code{terra::ext()}.}

\item{lon180}{logical, ensure Atlantic-centered longitude? Default TRUE.}

\item{magonly}{logical, return current speed only?}

\item{dironly}{logical, return current direction only?}

\item{uonly}{logical, return U (eastward) component only?}

\item{vonly}{logical, return V (northward) component only?}

\item{latest}{if TRUE and date missing, return latest; if FALSE, earliest.}

\item{returnfiles}{if TRUE, return file catalog instead of data.}

\item{...}{currently ignored.}

\item{inputfiles}{optional pre-filtered file catalog.}
}
\value{
\code{SpatRaster}. If default (no component flag), 2 layers named "U", "V".
With a component flag, single layer per date. Time dimension is set.
}
\description{
Read geostrophic current velocity from Copernicus/AVISO altimetry products.
}
\details{
Data contain U (eastward) and V (northward) velocity components. By default
both components are returned as a 2-layer SpatRaster. Use the component
arguments to return derived quantities instead:

\itemize{
\item \code{magonly = TRUE}: current speed (magnitude), sqrt(U² + V²)
\item \code{dironly = TRUE}: current direction (degrees, oceanographic convention)
\item \code{uonly = TRUE}: U component only
\item \code{vonly = TRUE}: V component only
}

Only one of these flags may be TRUE. When reading multiple dates with the
default (both U and V), only one date can be read at a time - use a component
flag to read time series.

Data are on a global 0.25° grid. Source orientation varies by file; use
\code{lon180 = TRUE} (default) to ensure Atlantic-centered output (-180 to 180).
}
\examples{
\dontrun{
# Latest currents (U and V)
uv <- read_copernicus_current_daily()

# Current speed only - can read multiple dates
speed <- read_copernicus_current_daily(c("2023-01-15", "2023-06-15"), magonly = TRUE)

# Direction
dir <- read_copernicus_current_daily("2023-01-15", dironly = TRUE)
}
}
\seealso{
\code{\link{readcurr}} for legacy interface
}
